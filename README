This is the rockchip modesetting-based DDXdriver.

Compiling
---------
	./autogen --prefix=/usr --enable-glamor
	make
	make install

CONF
---------
Section "Device"
        Identifier      "Rockchip"
        Driver          "modesetting"
        Option          "AccelMethod"           "disable-glamor"
        Option          "EXA"                   "true"
EndSection
